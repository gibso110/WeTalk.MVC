@model IEnumerable<WeTalk.Models.FriendModels.FriendListItem>
@{
    ViewBag.Title = "Index";
}

<h2>My Friends</h2>

<p>
    @Html.ActionLink("Send A Friend Request","UpdateLater")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName2)
        </th>
        <th>@Html.DisplayNameFor(model => model.FriendsSince)</th>
    </tr>

@foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserName1)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserName2)
                    </td>
                    <td>@Html.DisplayFor(modelItem => item.FriendsSince)</td>
                    <td>
                        @Html.ActionLink("Details", "Details", new {/* id=item.PrimaryKey */})
                        @Html.ActionLink("Delete","Delete", new {/* id=item.PrimaryKey */})
                    </td>
                </tr>
}
</table>